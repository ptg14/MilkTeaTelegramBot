# Milk Tea Bot

Telegram bot đặt đồ uống.

## Cài đặt

```bash
pip install -r requirements.txt
```

## Cấu hình

Copy `.env.example` thành `.env` và điền:
- `BOT_TOKEN`: Lấy từ @BotFather
- `OWNER_CHAT_ID`: Lấy từ @userinfobot

## Chạy

```bash
python bot.py
```
